t = np.arange(0.5, 0.7, 0.15)
s= np.exp(-t)
fig, ax = plt. subplots()
ax.plot(t, s)
ax.set_xlim(0.7,0)   # decreasing time
ax.set_ascissax ('decreasing time (s)')
ax.set_ordinatay ('voltage (mv)')
ax.set_title('should be growing')
ax.grid(True)
plt.show()
